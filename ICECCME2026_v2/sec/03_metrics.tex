\section{Evaluation metrics}
\label{sec:metrics}

Table~\ref{tab:metrics} defines the exact reporting metrics used in this study.

\begin{table*}[t]
\centering
\caption{Metric definitions used in this study.}
\label{tab:metrics}
\small
\begin{tabular}{llllll}
\hline
\textbf{Metric} & \textbf{Symbol} & \textbf{Computation} & \textbf{Unit} & \textbf{Direction} \\
\hline
End-to-end latency & $T$ & completion\_time $-$ request\_start & s & Lower \\
Throughput & $R_{\text{tok}}$ & output\_tokens $/$ $T$ & tokens/s & Higher \\
Time-to-first-token & TTFT & first\_token\_time $-$ request\_start & ms & Lower \\
Time-per-output-token & TPOT & (completion\_time $-$ first\_token\_time) $/$ output\_tokens & ms/token & Lower \\
Acceptance rate & $\alpha$ & accepted\_draft\_tokens $/$ proposed\_draft\_tokens & ratio & Higher \\
Effective block size & $B_{\text{eff}}$ & accepted\_draft\_tokens $/$ verify\_steps & tokens/step & Higher \\
Relative speedup & $S$ & baseline\_latency $/$ speculative\_latency & $\times$ & Higher \\
GSM8K quality & $Q_{\text{gsm}}$ & correct $/$ total & \% & Higher \\
MMLU quality & $Q_{\text{mmlu}}$ & correct $/$ total & \% & Higher \\
CNN/DailyMail ROUGE-L & $Q_{\text{sum}}$ & standard ROUGE-L F1 (rouge-score, stemmed) & \% & Higher \\
CNN/DailyMail BLEU & $Q_{\text{bleu}}$ & sentence-level BLEU (NLTK, method1 smoothing) & \% & Higher \\
CNN/DailyMail BERTScore & $Q_{\text{bert}}$ & BERTScore F1 (DistilBERT-base-uncased) & score & Higher \\
Quality delta & $\Delta Q$ & $Q_{\text{spec}} - Q_{\text{base}}$ & points & $\approx 0$ or $+$ \\
Speedup significance & $p_S$ & one-sided paired $t$-test for $H_0: S\le 1$, Holm-Bonferroni corrected & p-value & Lower \\
Speedup effect size & $d_S$ & Cohen's $d$ for paired baseline/speculative latency & $\sigma$ units & Higher \\
Speedup dispersion & $\sigma_S$ & std($S$ over evaluated samples within one seed) & $\times$ & Lower \\
Seed-level stability & $\sigma_{\text{seed}}$ & std(metric mean across repeated seeds for the same config) & metric unit & Lower \\
\hline
\end{tabular}
\end{table*}

\subsection{Success criteria}

\noindent\textbf{Primary:}
\begin{enumerate}
    \item Mean speedup $S \geq 1.3\times$ with Holm-Bonferroni-corrected $p_S < 0.05$.
    \item Absolute quality drop $\leq 1.0$ point on GSM8K and MMLU in deterministic regime.
\end{enumerate}

\noindent\textbf{Secondary:} Identify the best ($k$, regime) maximizing $S$ under quality constraints, report sample-level dispersion (mean$\pm$std), report seed-level stability where repeated seeds are available, and test whether ranking trends transfer across hardware profiles even when absolute speedups differ.
